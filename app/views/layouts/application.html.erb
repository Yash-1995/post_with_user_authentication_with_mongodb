<!DOCTYPE html>
<html>
  <head>
    <title>PostWithUserAuthenticationWithMongodb</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      crossorigin="anonymous"
    />
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light center">
    <div class="container py-4">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <nav class="mb-4 border-bottom pb-2">
        <% if user_signed_in? %>
          👋 Hello, <strong><%= current_user.email %></strong> |
          <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { turbo: false }, class: "btn btn-sm btn-outline-danger ms-2" %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-sm btn-outline-primary me-2" %>
          <%= link_to "Login", new_user_session_path, class: "btn btn-sm btn-outline-secondary" %>
        <% end %>
      </nav>
      <%= yield %>

      <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        
        crossorigin="anonymous"
      ></script>
  </body>
</html>
